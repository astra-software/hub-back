spring:
  datasource:
    url: jdbc:postgresql://${POSTGRE_HOST_DEV:localhost}:5432/hub_db
    username: root
    password: root
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    open-in-view: false
  sql:
    init:
      mode: always